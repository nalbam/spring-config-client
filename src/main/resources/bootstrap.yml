spring:
  application:
    name: nalbam
  profiles:
    active: ${PROFILE:default}
  cloud:
    config:
      uri: http://${CONFIG_SERVER_HOST:localhost}:${CONFIG_SERVER_PORT:8080}
      label: ${LABEL:master}
