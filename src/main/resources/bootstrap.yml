spring:
  application:
    name: nalbam
  profiles:
    active: ${PROFILE:default},push
  cloud:
    config:
      uri: http://${CONFIG_SERVER_HOST:localhost}:${CONFIG_SERVER_PORT:8888}
      label: ${LABEL:master}
